public with sharing class dev3_CTRL {
       public set<set<set<set<set<String>>>>> a; // 5 levels total
       public list<list<list<list<list<String>>>>> b; // 5 levels total
       public map<Id,map<Id,map<Id,map<Id,map<Id,map<Id,map<Id,String>>>>>>> c;
    public dev3_CTRL() {
        
        
    }
    public String ok() {return 'ddd';}
}